# notas clase 12

## Temas de la clase

- PHP
  - variables
  - concatenación de variables con strings (`"hola" . $nombre . ". ¿Cómo estás?"`, `"Hola $nombre . ¿Cómo estás?"` )
  - diferentes tipos de fatos
  - arrays y acceso a sus elementos
  - var_dump (`var_dump(array)`)
  - arrays asociativos
  - arrays de arrays asocitivos
  - foreach (`foreach($array as $elemento){ ... }`)
  - if, else, elseif (`if(){ ... } elseif(){ ... } else{ ... }`)
  - funciones (` function($entrada){ ... return $algo } `)
  - procedimientos (`function(){ ... }`)